---
description: "Implement a task — write code following project conventions and validate with build"
agent: "implementer"
argument-hint: "Describe the task to implement or reference a plan"
---

Implement the following task in the Jàngat project.

Search the codebase for existing patterns and match the style. Work in dependency order: schema → service → store → component → view. Run `npm run build` after changes to validate.
